# — Telegram API credentials —  
# These you get from https://my.telegram.org → API Development tools  
API_ID=your_api_id_here          # integer, e.g. 123456  
API_HASH=your_api_hash_here      # string, 32-character hex  

# — Bot helper —  
# If you are using a Bot account instead of user session, uncomment BOT_TOKEN  
BOT_TOKEN=your_bot_token_here  

# — Pyrogram Userbot Session —
# Leave the placeholder here. Generate a real string session using a script
# and put it ONLY into your private .env (never commit that file).
STRING_SESSION=your_session_string_here

# — Database configuration —
# This is only the database *name*. If your userbot uses SQLite,
# it will typically create something like your_database_name_here.db
# You can rename it to anything you want.
DB_NAME=your_database_name_here

# — Reply tracking group —
# This is the group/channel ID where reply logs will be stored.
# Must be a valid chat ID. For supergroups/channels, it starts with -100.
# Example: -1001234567890
REPLIES_ID=your_group_id_here